---
title: 'UTS DECISION TREE: CLASSIFICATION LAB'
author: " ... "
date: "April 2023, 2020"
output:
  html_notebook: 
    toc: yes
---

#Carseats {ISLR}

Sales of Child Car Seats
Description
A simulated data set containing sales of child car seats at 400 different stores.
  - Usage: Carseats
  - Format: A data frame with 400 observations on the following 11 variables.
  - Sales: Unit sales (in thousands) at each location
  - CompPrice: Price charged by competitor at each location
  - Income: Community income level (in thousands of dollars)
  - Advertising: Local advertising budget for company at each location (in thousands of dollars)
  - Population: Population size in region (in thousands)
  - Price: Price company charges for car seats at each site
  - ShelveLoc: A factor with levels Bad, Good and Medium indicating the quality of the shelving location for the car seats at each site
  - Age: Average age of the local population
  - Education: Education level at each location
  - Urban: A factor with levels No and Yes to indicate whether the store is in an urban or rural location
  - US: A factor with levels No and Yes to indicate whether the store is in the US or not
  - Source: Simulated data

References
James, G., Witten, D., Hastie, T., and Tibshirani, R. (2013) An Introduction to Statistical Learning with applications in R, www.StatLearning.com, Springer-Verlag, New York


# Load libraries and data

```{r}
#Load tree library to construct classification trees
library("tree")

#Load library contains the Carseats data set. 
library("ISLR")

#Load the Carseats data
attach(Carseats)
#View(Carseats)
head(Carseats, 5)
```


# Create vector High that represent high sales and merge with original Carseats. 
Saudara tidak memutuskan berapa threshold untuk menyatakan sales High tapi menyerahkan ke pada random number antara rand1 dan rand2. TETAPKANLAH rand1 dan rand2 antara 7 dan 9.
```{r cache=TRUE}
library(dplyr)

rand1=6
rand2=9
threshold <- runif(1, rand1, rand2)
threshold

Carseats$Sales = sample(Carseats$Sales)
Carseats = Carseats %>%
  mutate(High = as.factor(ifelse(Sales <= threshold, "No", "Yes")))
head(Carseats)

attach(Carseats)
```

# =========================================Descriptive

```{r}
library(dplyr)
Carseats %>%
  dplyr::mutate(ShelveLoc=as.factor(ShelveLoc)) %>%
  GGally::ggpairs(columns=c(1,4),
        mapping=ggplot2::aes(colour=ShelveLoc, alpha=0.5),
                diag=list(continuous="density",
                          discrete="bar"),
                upper=list(continuous="cor",
                           combo="box",
                           discrete="ratio"),
                lower=list(continuous="points",
                           combo="denstrip",
                           discrete="facetbar")) +
  ggplot2::theme(panel.grid.major=ggplot2::element_blank())
```

Gambar di atas memperlihatkan densitas dari Sales dan Advertising yang dikelompokkan menurut ShelveLoc
# ==========================================

# Pertanyaan 1 
Berikan pendapat Saudara tentang densitas Sales!

Jawaban:
Pada kurva densitas Sales, diperlihatkan puncak dari ketiga indikator Bad, Good dan Medium saling berdekatan satu sama lain di sumbu y pada interval +- 0,15.Secara visual, indikator medium yang memiliki nilai tertinggi, ini menandakan bahwa rak dengan lokasi yang sedang, tidak terlalu bagus maupun buruk, memiliki penjualan yang tertinggi dibandingkan rak-rak dengan lokasi yang bagus maupun buruk. 
...

# ==========================================


# Pertanyaan 2 
Berikan pendapat Saudara tentang densitas Advertising!
Pada kurva densitas Advertising, 
Jawaban: 
Pada kurva densitas Advertising, diperlihatkan puncak dari ketiga indikator Bad, Good dan Medium saling berdekatan satu sama lain di sumbu x pada interval 0 sampai 2 dan terlihat memiliki lebih dari satu segmen, di mana segmen lainnya memiliki puncak di sumbu x pada interval 10 sampai 13. Secara visual, indikator medium yang memiliki nilai tertinggi, ini menandakan bahwa rak dengan lokasi yang sedang, tidak terlalu bagus maupun buruk, memiliki pengiklanan yang tertinggi dibandingkan rak-rak dengan lokasi yang bagus maupun buruk.
...

# ==========================================


# Pertanyaan 3
Berikan pendapat Saudara tentang hubungan Sales dan Advertising!

Jawaban:
Pada bagian korelasi, diperlihatkan bahwa korelasi secara keseluruhan antara Sales dan Advertising memiliki nilai -0,004. Nilai korelasi ini menunjukkan bahwa hubungan antara Sales dan Advertising hampir mendekati 0, yang menandakan bahwa hampir tidak adanya hubungan yang berarti antara Sales dan Advertising. Secara parsial pun indikator Good, Bad dan Medium memiliki nilai korelasi yang rendah, hal ini menandakan bahwa hampir tidak adanya hubungan yang signifikan antara Sales dan Advertising.
...

# ==========================================

```{r}
Carseats %>%
  dplyr::mutate(High=as.factor(High)) %>%
  GGally::ggpairs(columns=c(2,6),
        mapping=ggplot2::aes(colour=High, alpha=0.5),
                diag=list(continuous="density",
                          discrete="bar"),
                upper=list(continuous="cor",
                           combo="box",
                           discrete="ratio"),
                lower=list(continuous="points",
                           combo="denstrip",
                           discrete="facetbar")) +
  ggplot2::theme(panel.grid.major=ggplot2::element_blank())
```

Gambar di atas memperlihatkan densitas dari CompPrice dan Price yang dikelompokkan menurut High.

#==========================================

#Pertanyaan 4
Berikan pendapat Saudara tentang hubungan Competitor Price dan (Own) Price!

Jawaban:
Pada bagian korelasi, diperlihatkan bahwa Price dan CompPrice memiliki nilai korelasi positif sebesar 0,585, ini merupakan nilai korelasi yang cukup tinggi, yang menandakan bahwa jika Price meningkat, maka CompPrice juga akan meningkat dan begitu pun sebaliknya.
...

#==========================================

# =========================================
#Predictive - Classification


Split dataset into train-test
```{r cache=TRUE}
#Dataset for training
n = runif(1, 200, 300)
train=sample (1: nrow(Carseats ), n)  #Row sample for training data
head(train)

#Dataset for testing
Carseats.test = Carseats [-train ,]
```


#Training
Do the classification tree for Carseats using  training data.
```{r cache=TRUE}
set.seed(123)
tree.carseats = tree(High ~ .-Sales, Carseats, subset = train )

#Summarize the classification tree model
summary(tree.carseats)
```

#==========================================

#Pertanyaan 5
Berikan pendapat Saudara tentang summary dari model tree tersebut!

Jawaban:
Dari model tree yang digunakan, terlihat bahwa variabel-variabel yang berpengaruh penting dalam model adalah ShelveLoc atau posisi rak, Education atau pendidikan dari populasi lokal, Population atau jumlah konsumen dalam suatu wilayah, Age atau usia populasi lokal, CompPrice atau harga kompetitor pada suatu wilayah, Advertising atau pengiklanan, Price atau harga, dan juga income atau pendapatan perusahaan. Node yang dipkai berjumlah 20 dengan deviasi rata-rata residunya adalah 0,9208 dan tingkat kesalahan klasifikasinya sebesar 0,2452 atau 24,52%.
...

#==========================================


```{r}
#Text visualization of model
tree.carseats

#Tree visualization of model
plot(tree.carseats )
text(tree.carseats ,cex=1/2)
```


#==========================================

# Pertanyaan 6
Pilih cabang yang menarik bagi Saudara dan jelaskan!
Mahasiswa boleh memilih cabang mana saja asal menjelaskan alasannya. Dalam realitas ini menggambarkan proses pengambilan keputusan dan alasan yang mendasarinya.

Jawaban:
Saya memilih cabang 2,4,8,16 dan 34. Karena pada cabang tersebut diterangkan bahwa produk memiliki taraf pendidikan konsumen yang tidak terlalu tinggi yaitu di bawah 15.5, lalu dengan harga yang ditawarkan juga di bawah 99.5. Kemudian setelah diterapkan harga demikian, syarat tingkat pendidikan konsumen menurun kembali menjadi di bawah 13.5 dan meningkatkan populasi konsumen yang potensial menjadi di bawah 207.5. Dengan semua kondisi di atas, di tambah dengan lokasi rak (ShelveLoc) yang medium, dapat meningkatkan penjualan dengan lebih signifikan, ditandai dengan adanya perannya cabang tersebut sebagai terminal node dengan pucuk "No". 


#==========================================

#Evaluate the decision tree model using test data.
```{r cache=TRUE}
#Predict the model using data testing Carseats.test
set.seed(123)
tree.pred=predict(tree.carseats , Carseats.test, type ="class")
head(tree.pred, 5)

#Generate the confusion matrix showing proportions from testing data .
print("confusion matrix showing test proportion")
per <- rattle::errorMatrix(Carseats.test$High, tree.pred, count = FALSE)
per

#Calculate the testing overall error percentage.
print("overall error percentage")
cat(100-sum(diag(per), na.rm=TRUE))
```


#==========================================

# Pertanyaan 7
Sebagai pengambil keputusan, berikan jugdement Saudara berkaitan dengan target perusahaan mengenai nilai True Positive yang didapatkan dari hasil pengujian model menggunakan data testing! Bandingkan dengan model training di atas (kaitkan dengan jawaban pertanyaan no 6 di atas).

Jawaban:
Berdasarkan hasil uji menggunakan confusion matrix, didapatkan nilai True Positive nya adalah 12.6, ini adalah angka yang sangat rendah untuk nilai True Positive, dan karenanya nilai overall error percentage yang didapat juga sangat rendah yaitu 40.4%. Walaupun demikian, hasil ini masih sesuai dengan pengambilan keputusan pada nomo 6 yang menunjukkan hasil akhir yang memiliki pucuk "No"
...

#==========================================

Calculate the testing overall error percentage.
```{r cache=TRUE}
print("overall error percentage")
cat(100-sum(diag(per), na.rm=TRUE))
```
#==========================================

# Pertanyaan 8
Menggunakan kriteria hasil testing ("overall error percentage"), sebagai pengambil keputusan, berikan jugdement Saudara berkaitan dengan target perusahaan mengenai nilai True overall accuracy! Berapa kira2 asumsi yang acceptable menurut Saudara?

Jawaban:
Nilai overall error percentage yang didapat dari hasil simulasi sebesar 40,4%. Menurut saya, persentase ini masihlah cukup besar, karena dengan 40,4% overall error percentage, maka akurasi yang dimiliki oleh pohon yang digunakan hanya 59,6%. Nilai ini jauh lebih rendah jika dibandingkan dengan penelitian yang dilakukan oleh Jijo & Abdulazeez (2021) yang memiliki akurasi sebesar 99,93% dengan menggunakan dataset dari repository machine learning.
...

#==========================================

#==========================================

# Pertanyaan 9

Berdasarkan analisis pruning atas model training di bawah ini, apakah Saudara akan melakukan pruning?
Jelaskan alasan Saudara!

(Catatan, pruning atas model decision tree dilakukan apabila memberikan keuntungan dari sisi "Number of terminal nodes" dan "overall accuracy")

Jawaban:
Dengan hasil overall error percentage yang didapat sebelumnya, peneliti akan melakukan pruning untuk memotong beberapa cabang untuk meningkatkan Number of terminal nodes dan juga overall accuracy.
...

#==========================================

#Pruning the tree
Berikut adalah langkah-langkah analisis pruning

1. Gunakan kriteria cross validation untuk melakukan pruning
```{r cache=TRUE}
#Calculate deviance
set.seed (123)
cv.carseats = cv.tree(tree.carseats, FUN=prune.misclass, K = 10)
cv.carseats

#We plot the error rate as a function of both size and k.
par(mfrow=c(1,2))
plot(cv.carseats$size ,cv.carseats$dev ,type="b")
plot(cv.carseats$k ,cv.carseats$dev ,type="b")
```


2. Menggunakan grafik tersebut di atas, tetapkan best sizenya lalu lakukan pruning dan visualisasikan.
Apakah ada perbaikan jumlah daun dibandingkan dengan model sebelum pruning.

```{r}
#Prune the best size
set.seed(123)
best_size = 5
prune.carseats = prune.misclass(tree.carseats , best = best_size)

#Summary of pruning result
summary(prune.carseats)

#Text visualization
prune.carseats

#Tree visualization
plot(prune.carseats )
text(prune.carseats ,col=1, cex=1/2)
```
`

3. Ujikan hasil model pruning menggunakan test data set. 
```{r}
#Predict using pruned tree model
set.seed(123)
tree_pred = predict(prune.carseats, Carseats.test, type="class")

#Generate the confusion matrix showing counts for pruned tree.
print("confusion matrix showing test counts")
per_pruned <- rattle::errorMatrix(Carseats.test$High, tree_pred, count=FALSE)
per_pruned

#Overall error
print("overall error percentage")
cat(100-sum(diag(per_pruned), na.rm=TRUE))
```


# Prediksi

#==========================================

# Pertanyaan 10

Toko dengan kondisi sebagai berikut: "CompPrice=115", "Income=28", "Advertising=11", "Population=29", "Price=86", "ShelveLoc=Good ", "Age=53", "Education=18", "Urban=Yes ", "US=Yes". 

Lakukan prediksi apakah “Sales=High” atau “Sales=Low”!

Jawaban:
a. Tanpa pruning ...
b. Dengan pruning ...

#==========================================

Jawab:
a. Tanpa pruning
```{r}
# Create a data frame with 400 rows
newdata <- data.frame(
  CompPrice = rep(115, 400),
  Income = rep(28, 400),
  Advertising = rep(11, 400),
  Population = rep(29, 400),
  Price = rep(86, 400),
  ShelveLoc = rep("Good", 400),
  Age = rep(53, 400),
  Education = rep(18, 400),
  Urban = rep("Yes", 400),
  US = rep("Yes", 400)
)
newdata$ShelveLoc<-as.factor(newdata$ShelveLoc)
newdata$Urban<-as.factor(newdata$Urban)
newdata$US<-as.factor(newdata$US)
# Predict using the new data
set.seed(123)
# Predict using unpruned tree model
unpruned_pred <- predict(tree.carseats, newdata, type="class")

# Predict using pruned tree model
pruned_pred <- predict(prune.carseats, newdata, type="class")

# Print the predictions
unpruned_pred_output<-cat("Unpruned tree prediction: ", unpruned_pred, "\n")
unpruned_pred_output<-cat("Pruned tree prediction: ", pruned_pred)


summary(unpruned_pred)

summary(pruned_pred)

```


